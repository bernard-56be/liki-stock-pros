// Re-export the server action and types so owner components can import from '@/owner/dashboard/dashboardService'
export { fetchDashboardData, type DashboardData } from '@/lib/actions/dashboardService (1)';
